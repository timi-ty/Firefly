using UnityEngine;

namespace Firefly.Core.GOManagement
{
    public class GameObjectPool : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
